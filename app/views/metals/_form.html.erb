<%= form_with(model: metal, local: true) do |form| %>
  <% if metal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(metal.errors.count, "error") %> prohibited this metal from being saved:</h2>

      <ul>
      <% metal.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :metal_name %>
  </div>

  <div class="field">
    <%= form.label :metal_type %>
    <%= form.text_field :metal_type, id: :metal_metal_type %>
  </div>

  <div class="field">
    <%= form.label :metal_accents %>
    <%= form.text_field :metal_accents, id: :metal_metal_accents %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
